# Python Examples

Install dependencies:

```bash
pip install -r requirements.txt
```

Run the iOS yeemiao sign demo:

```bash
python ios_yeemiao_get_sign.py
```

This calls:

```text
POST /rpc/invoke/yeemiao/get_sign
```

The old Sekiro `get_sign2` action is now `get_sign` in the iOS R0RPC demo.
